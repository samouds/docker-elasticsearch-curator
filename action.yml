actions:
  1:
    action: delete_indices
    description: delete indices
    filters:
    - filtertype: age
      source: creation_date
      direction: older
      unit: days 
      unit_count: ${OLDER_THAN_IN_DAYS}
